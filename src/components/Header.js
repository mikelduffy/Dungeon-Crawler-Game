import React from 'react';
import {PageHeader} from 'react-bootstrap';

const center = {
  textAlign: 'center',
};

const Header = function() {
  return (
    <PageHeader style={center}>
      Dungeon Crawler
    </PageHeader>
  );
};

export default Header;
